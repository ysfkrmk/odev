#include <stdio.h>

void main() {
    int x, y, z;
    
    printf("Sayi yezin: ");
    scanf("%d", &x);
    printf("\nSayi yazin: ");
    scanf("%d", &y);
    
    if (x>b) {
        
    } else {
        x = z;
        x = y;
        y = z;
    }
    
    while(y<x) {
        printf("\n%d", y*y);
        y++;
    }
}