#include <stdio.h>

void main () {
    int toplam=0, sayi, sayac=1;
    
    printf("Bir sayi giriniz: ");
    scanf("%d", &sayi);
    
    while(sayac<sayi {
        toplam += sayac;
        sayac++;
    }
    printf("%d sayilarin toplamidir", toplam);
}